def soma(x: float, y: float) -> float:
    return x + y


if __name__ == '__main__':  # só executa se estiver rodando este arquivo.
    print(__name__)  # no próprio arquivo, o nome é __main__.
    print(soma(10, 40))
    print(soma(40, 40))
